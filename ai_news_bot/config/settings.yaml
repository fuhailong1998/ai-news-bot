storage:
  db_path: storage/seen.db
  retention_days: 30
  first_run_window_days: 7   # 全局新鲜度窗口：published_at 老于 N 天的条目静默丢弃，避免老内容被延迟推送（HF / RSS 偶发情况）

push:
  per_run_limit: 10        # 每轮最多推送几条，避免刷屏
  interval_seconds: 0.8    # 每条之间间隔（飞书限速）
  max_retry: 3

summarizer:
  enabled: true            # 开启 LLM 中文摘要
  model: deepseek-chat
  max_input_chars: 2000
  max_summary_chars: 80

fetch:
  timeout_seconds: 20
  user_agent: "Mozilla/5.0 (compatible; AINewsBot/0.1)"
  concurrency: 8
